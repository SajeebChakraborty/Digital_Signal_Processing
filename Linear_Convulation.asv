x=input('Enter the Signal Sequence');
h=input('Enter the Inpulse Sequence');

n1=length(x);
n2=length(h);

X=[x,zeros(1,n2)];
H=[h,zeros(1,n1)];

for i=1:n1+n2-1
    y[i]=0;
    for j=1:n1
       
        y[i]=y[i]+
        
        
    end    
end
